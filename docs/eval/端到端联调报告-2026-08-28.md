# DevMind AI 端到端联调报告

日期：2026-08-28

## 环境

- PostgreSQL 16、Redis 7.2、MinIO、etcd、Milvus 2.4.4：健康
- 后端：`http://127.0.0.1:15200`
- 前端：`http://127.0.0.1:5173`
- Embedding：本地 BGE-M3
- Reranker：本地 bge-reranker-v2-m3
- LLM：DashScope `qwen-plus`

## 数据库迁移

新增 `sql/migrations/004_workspace_favorites_preferences.sql`，包含：

- `message_favorites`
- `user_preferences`
- `idx_message_favorites_user_recent`

迁移在现有 PostgreSQL 数据卷连续执行两次成功，满足幂等要求。

## 真实链路结果

测试脚本：`uv run python tests/live_e2e.py`

```text
注册/登录 → 创建知识库 → 保存偏好 → 上传 Markdown
→ MinIO → 文档解析 → 3 个分块 → BGE-M3 → Milvus
→ 混合检索 → BGE Reranker → WebSocket 流式回答
→ 3 条引用 → 收藏/取消收藏 → 历史恢复/重命名 → 退出失效
```

结果：

- 文档任务：`completed`，进度 100%
- 文档状态：`ready`
- 分块数量：3
- 检索召回：3
- Top-1 重排分数：0.9945951
- 流式回答长度：534 字符
- 引用数量：3
- 收藏、偏好、历史和退出黑名单：通过
- 展示页 CTA、登录页、手机登录/注册切换：通过
- 浏览器控制台错误：0

## 回归结果

- 后端：39 passed，4 subtests passed
- 前端：Vite production build 通过
- `git diff --check`：通过（仅 Windows 换行提示）

## 已发现但未在本阶段调整的问题

本次回答 Top-1 重排分数为 0.9945951，但另外两条为 0.1080265、0.0602780。当前可信度使用全部引用分数的简单平均，因此最终判定为 `low`。该现象不阻塞链路，但说明可信度公式和引用截断阈值需要基于评测集校准，不能直接凭单个样本修改。
