# 企业内部技术知识库智能问答系统 · 任务分工

- 日期：2026-08-21
- 项目类型：RAG MVP（先跑通闭环，后续迭代）
- 团队规模：10 人
- 技术基线：复用本仓库「黑马课程问答系统」架构（FastAPI + MySQL/BM25 + Redis + Milvus + BGE-M3 + bge-reranker-v2-m3 + DashScope LLM + React）

---

## 1. 项目概述

### 1.1 专属问题（一句话定义）

> 解决「**研发/运维/支持人员在多套内部系统（Wiki、代码库、接口文档、值班手册）中定位技术资料慢、重复提问、新人上手慢**」的问题。

### 1.2 需求真实性依据（公开数据，可复查）

| 证据 | 来源 |
|------|------|
| DXC 用 AI 支持实现 **58% 工单分流**，无需人工介入 | [Resolve 案例](https://resolve.io/customers/dxc-58-percent-deflection-ai-it-support) |
| 企业工单分流行业基准为 15–30%，**38% 企业**已将"IT 自助服务与工单分流"列为优先事项 | [Mosaic AI](https://getmosaic.ai/blog/truth-about-ticket-deflection)、[Aisera](https://www.sgpjbg.com/labelsyh/itzizhufuwuyugongdanfenliucelue.html) |
| Alteryx 用生成式 AI 扩展企业内部 IT 支持 | [Resolve 案例](https://resolve.io/customers/alteryx-ai-virtual-agent-it-support) |
| 内部文档碎片化被称为"每个工程团队隐藏的生产力杀手" | [Doc Holiday](https://doc.holiday/blog/the-hidden-productivity-killer-in-every-engineering-team) |

### 1.3 MVP 量化目标（整体验收线，见第 7 节）

- 高频技术问题自动解答率 ≥ 80%
- 每条回答 100% 附带文档出处引用
- 平均响应时长 < 3 秒（流式首 token < 1.5 秒）
- 低置信度问题 100% 触发转人工兜底（宁可不答，不错答）
- 权限隔离 100% 生效（越权检索命中数为 0）

### 1.4 数据源（企业内部，授权获取）

Confluence/Wiki 技术文档、Git 仓库文档（README/设计文档/接口文档）、值班手册（runbook）、故障复盘报告、脱敏后的历史工单与 FAQ。全部在企业域内处理，数据不出域。

> 先选 **1–2 个团队/系统**试点（如后端组 + 1 个核心系统的文档全量入库），跑通后再推广到全公司。

---

## 2. 数据集获取方案（具体可执行）

### 2.1 企业内部权威数据源（授权获取，主知识库）

| 数据源 | 具体内容 | 获取方式 | 合规性 |
|--------|----------|----------|--------|
| Confluence/Wiki | 架构设计、技术方案、流程规范、环境配置、常见问题 | 官方 REST API / 导出（需管理员授权），增量同步 | 企业内部授权，零外部风险 |
| Git 仓库 | README、设计文档、接口文档、CHANGELOG、代码注释 | `git clone` / GitLab-GitHub API 拉取文档类文件 | 企业内部授权 |
| 接口文档 | Swagger/OpenAPI 规范、内部接口文档（本仓库已有 `接口文档.md`） | 直接从仓库/接口平台导出 | 企业内部授权 |
| 值班手册/runbook | 故障处理步骤、告警响应流程、值班须知 | 内部系统导出 | 企业内部授权 |
| 历史工单/FAQ | 高频问题、故障复盘报告 | 工单系统导出后**脱敏** | 需脱敏处理（见 2.3） |

### 2.2 开源数据集（用途：训练意图分类器、评测 FAQ 匹配与检索链路）

> 说明：内部技术知识库是企业私有数据，无公开数据集；以下开源数据集用于辅助组件（分类器/匹配/评估），不进入主知识库：

| 数据集 | 内容 | 用途 | 获取地址 |
|--------|------|------|----------|
| SMP2017-ECDT | 中文人机对话意图分类语料 | 训练 T6 意图分类器（技术/权限申请/故障上报类） | [hyper.ai 数据集平台](https://beta.hyper.ai/cn/datasets/29515)（原 SMP2017 公开赛数据） |
| LCQMC | 哈工大 26 万对中文句对语义匹配语料 | 评测 FAQ 相似度匹配效果 | 哈工大 SCIR 发布，GitHub 镜像（可搜 `lcqmc`，[中文语料汇总](https://github.com/WuhanLLLiu/ChineseNLPCorpus) 内含下载链接） |
| DuReader | 百度中文机器阅读理解（真实应用来源） | 评测检索+阅读链路质量 | [百度官方 GitHub](https://github.com/baidu/DuReader) |
| CMRC 2018 | 中文机器阅读理解 | 评测答案抽取能力 | [官方 GitHub](https://github.com/sunyuqingannie/cmrc2018) |
| THUCNews / tnews | 中文文本分类语料 | 分类器预训练/迁移学习 | GitHub（清华）/ [CLUE benchmark](https://github.com/CLUEbenchmark/CLUE) |
| 本仓库已有数据 | 黑马教育课程咨询知识库、FAQ、分类器训练数据 | 直接复用验证架构、Prompt 与评估脚本 | 本仓库 `rag_qa/data/` |

### 2.3 内部数据合规处理与安全规范

企业场景的核心不是"爬取"，而是**脱敏、权限、不出域**：

**数据处理规则表：**

| 规则 | 具体做法 |
|------|----------|
| 敏感信息脱敏 | 入库前用正则/命名实体识别剔除：密钥、Token、密码、内网 IP/账号、客户数据、个人信息、薪酬数据 |
| 权限分级 | 文档标注 `team`（所属团队）、`security_level`（公开/团队/机密）元数据，检索时按用户权限过滤 |
| 私有化部署 | 向量库、应用、LLM 全部在企业域内；若用外部 LLM API，仅传检索到的文档片段且先经脱敏 |
| 授权边界 | 只入库已获授权的团队/系统文档；不绕过内部系统权限拉取 |
| 审计留痕 | 查询日志记录用户、问题、命中文档，满足合规审计 |

**绝对不做清单：**

- ❌ 不把密钥/生产配置/客户数据/个人信息写入知识库
- ❌ 不采集未授权团队或系统的文档
- ❌ 不用公网大模型传输未经脱敏的内部文档全文
- ❌ 不做基于知识库的自动代码生成/自动运维变更（MVP 外）
- ❌ 不向无权限用户返回受限文档内容

**合规依据：** 数据来源均为企业自有、已授权文档；配合脱敏管线 + RBAC 权限过滤 + 私有化部署 + 审计日志，满足企业内部信息安全基线要求。

### 2.4 FAQ 构建与数据更新

| 事项 | 做法 |
|------|------|
| FAQ 来源 | 历史工单高频问题（脱敏）+ 团队常见问题 + Wiki 已有 FAQ 栏目 |
| 元数据规范 | 所有文档带 `team`、`system`、`doc_type`、`version`、`last_updated`、`security_level` |
| 版本管理 | 文档更新触发增量重建（Confluence/Git webhook 或定时任务），过期文档标记失效 |
| 闭环更新 | 线上差评/转人工问题每周人工复核，确认后补充进 FAQ 与知识库 |

---

## 3. 需求清单（R1–R9）

| 编号 | 需求描述（可量化） |
|------|--------------------|
| R1 | 支持 Confluence / Word / Markdown / PDF / OpenAPI 多格式内部文档解析，输出规范化纯文本 |
| R2 | 文档按语义切分并标注元数据（团队、系统、文档类型、版本、权限等级），Embedding 后写入 Milvus |
| R3 | 高频技术问题（环境配置、权限申请、常见报错等）走 MySQL 精确匹配，秒级直回 |
| R4 | 非标准问题走「向量检索 → 重排 → LLM 生成」主链路，回答必须附文档出处引用 |
| R5 | 对用户问题做意图分类（技术问题 / 权限申请 / 故障上报 / 闲聊 / 投诉），路由不同处理策略 |
| R6 | 提供 HTTP/WS 问答接口 + 前端/企业 IM 问答入口（引用展示、点赞点踩、转人工入口） |
| R7 | 低置信度 / 投诉类问题进入转人工与工单联动队列，用户反馈回写知识库形成闭环 |
| R8 | 构建 50–100 条高频技术问题评测集，用 RAGAS 四指标量化评估并输出报告 |
| R9 | docker-compose 一键私有化部署全部依赖与前后端，支持压测、监控与权限隔离验证 |

---

## 4. 任务清单（T1–T14）

> 「可优化点」表格说明每个任务当前技术的局限，以及后续用什么技术优化（MVP 阶段先按当前方案实现，优化点不进 MVP）。
> 「检验标准」按仓库规范只列检验方面与指标，**不写阈值**（阈值统一在第 7 节）。

### T1. 采集整理内部技术文档与 FAQ

- 目标：产出规范化技术文档集与高频 FAQ 表，完成脱敏与权限标注
- 实现技术方案：按 2.1 用 Confluence API / git clone / 系统导出采集文档；按 2.3 执行脱敏管线；FAQ 按 2.4 构建；文档按 team / system / security_level 标注
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 手工导出 + API 拉取 + 规则脱敏 | 多系统接入重复开发、脱敏规则易漏 | Confluence/Git 原生 webhook 增量同步；LLM + 正则双通道脱敏校验 |

- 对外接口：无（交付物：`internal_kb_qa/data/raw/` 文档集 + `internal_kb_qa/data/faq.xlsx` + 脱敏与权限标注表）
- 检验标准：文档完整性（目标团队/系统全覆盖）、脱敏漏检率、FAQ 条数与人工核对准确率、元数据标注覆盖率
- 对应需求：R1、R3

### T2. 实现多格式文档解析器

- 目标：把 Confluence / Word / Markdown / PDF / OpenAPI 文档统一解析为标准 Document 对象
- 实现技术方案：pymupdf + python-docx + unstructured + BeautifulSoup（Confluence 导出 HTML）+ 已有 Swagger/OpenAPI JSON 解析；复用 `rag_qa/edu_document_loaders/` 现成 loader
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| pymupdf / unstructured 纯文本提取 | 代码块、表格、架构图丢失，解析质量参差 | [MinerU](https://github.com/opendatalab/MinerU)（PDF 转 Markdown/JSON 高质量提取）；代码块单独按语言切分保留结构；架构图走 OCR/多模态模型 |

- 对外接口：
  - `load(file_path: str) -> list[Document]`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | file_path | string | 是 | 文档文件路径（.pdf / .docx / .md / .html / .json） |

    出参（Document 对象字段）：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | page_content | string | 解析出的纯文本（代码块保留结构标记） |
    | metadata.source | string | 来源文件/页面名 |
    | metadata.page | int | 所在页码（单页文档为 1） |
    | metadata.doc_type | string | 文档类型：wiki / api / runbook / faq / report |

- 检验标准：各格式解析正确性（文本完整率、乱码率）、代码块结构保留率、失败文档报错可定位
- 对应需求：R1

### T3. 实现文档切分与向量化入库

- 目标：把技术文档切分为语义完整、元数据齐全的 Chunk，并使用 BGE-M3 向量化后写入 Milvus
- 实现技术方案：LangChain RecursiveCharacterTextSplitter + 标题层级切分；代码块按函数/类边界切分（Code Splitter）；元数据新增 `team`、`system`、`version`、`last_updated`、`security_level`；sentence-transformers（BGE-M3）+ pymilvus 写入 collection `internal_tech_kb`，参考 `rag_qa/core/vector_store.py`
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 固定窗口字符切分 | 代码与配置片段被截断，技术语义被破坏 | 语义分段模型（本仓库已有 `nlp_bert_document-segmentation-chinese-base`）；代码按 AST 函数边界切分；父子块（parent-child chunk）索引 |

- 对外接口：
  - `split(documents: list[Document]) -> list[Chunk]`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | documents | array | 是 | T2 输出的 Document 列表 |

    出参（Chunk 对象字段）：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | text | string | 切分后的文本块 |
    | metadata | object | 继承 source / page / doc_type，新增 team、system、version、last_updated、security_level |

  - `insert(chunks: list[Chunk]) -> int`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | chunks | array | 是 | split() 输出的 Chunk 列表 |

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | count | int | 成功写入条数 |

- 检验标准：切分粒度分布、技术语义完整性（代码/配置不截断）、元数据覆盖率、向量维度与 collection 一致性、入库成功率与写入延迟
- 对应需求：R2

### T4. 实现混合检索扩展

- 目标：在 T3 稠密向量入库基础上扩展 BM25 + Dense 混合检索（RRF 融合），Milvus 向量库支持 top-k 检索与权限/系统元数据过滤
- 实现技术方案：Milvus 原生 BM25 全文检索 + Dense 向量检索 + RRF 融合（hybrid search）；collection 沿用 `internal_tech_kb`；检索时强制注入 `team`、`system`、`security_level` 过滤；多租户 partition 隔离；参考 `rag_qa/core/vector_store.py`
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| BM25 + Dense 固定权重 RRF 融合 | 不同问法下关键词与语义权重差异大，术语与口语问法仍有鸿沟 | Query 改写与检索策略路由（T13）；按文档类型/团队自适应调权；文档与代码双路召回 |

- 对外接口：
  - `search(query: str, top_k: int, filter: dict = None) -> list[Hit]`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | query | string | 是 | 用户问题（可传入 T13 改写后的 query） |
    | top_k | int | 是 | 召回条数 |
    | filter | dict | 否 | 元数据过滤（team、system、security_level，权限过滤必传） |

    出参（Hit 对象字段）：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | text | string | 命中的文档文本块 |
    | score | float | RRF 融合得分 |
    | metadata | object | 团队、系统、文档类型、版本、更新时间、权限等级 |

- 检验标准：混合检索召回率、关键词与语义混合命中质量、检索延迟、权限过滤正确性（越权命中数）
- 对应需求：R4

### T5. 实现高频技术 FAQ 精确匹配

- 目标：标准高频技术问题走 MySQL + BM25 精确/模糊匹配，命中即秒回，不进 LLM
- 实现技术方案：MySQL + BM25Search，复用 `mysql_qa/`（表结构：问题、标准答案、文档出处、类别、所属团队）
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| BM25 关键词匹配 | 口语化/同义改写命中差（"环境装不上" vs "依赖安装失败排查"） | Sentence-BERT 向量相似度匹配 + 阈值路由；LLM 生成同义问句扩充 FAQ 库 |

- 对外接口：
  - `faq_match(query: str) -> FAQHit | None`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | query | string | 是 | 用户问题 |

    出参（未命中返回 None）：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | answer | string | 标准答案 |
    | source | string | 答案出处（文档名/链接） |
    | score | float | 匹配置信度，低于阈值返回 None 走 RAG |

- 检验标准：FAQ 命中率、误匹配率（答非所问比例）、响应延迟
- 对应需求：R3

### T6. 实现意图分类与路由

- 目标：把用户问题分为五类并给出置信度，供主链路路由
- 实现技术方案：微调 BERT 分类器（复用 `rag_qa/core/bert_query_classifier/` 流程），类别：tech / access_request / incident / complaint / chitchat；训练数据用 2.2 的 SMP2017 + 自标注内部语料
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 单模型微调 | 标注数据少、"技术问题"与"故障上报"边界模糊 | LLM few-shot 分类兜底低置信度样本；主动学习挑难例降低标注量 |

- 对外接口：
  - `classify(query: str) -> Classification`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | query | string | 是 | 用户问题 |

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | category | string | tech / access_request / incident / complaint / chitchat |
    | confidence | float | 分类置信度，低于阈值按 tech（保守路由到带引用的 RAG）处理 |

- 检验标准：分类准确率、各类别召回率、低置信度拒识率
- 对应需求：R5

### T7. 实现重排与生成回答

- 目标：对 T4 召回结果做 bge-reranker 重排 → 带引用 Prompt 生成，输出答案与文档出处
- 实现技术方案：bge-reranker-v2-m3 + DashScope LLM（OpenAI 兼容，流式）；Prompt 要求「仅依据检索到的文档回答 + 标注文档出处 + 文档未覆盖时明确说不知道并建议转人工」；T13 检索策略作为可插拔增强，MVP 先跑通单路，随后接入
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 单次检索 + 重排 | 跨系统问题（"A 服务调 B 接口报错"）需多文档联动；代码与文档需双路召回 | 文档+代码双路检索融合；接入 T13 查询改写与多路召回策略；系统依赖图谱（GraphRAG：服务→接口→文档→负责人）；Self-RAG 自反思 |

- 对外接口：
  - `rag_answer(query: str, history: list = None) -> RAGResult`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | query | string | 是 | 用户问题 |
    | history | array | 否 | 最近 N 轮问答，用于补全指代 |

    出参（RAGResult 对象字段）：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | answer | string | 生成答案（流式时逐 token 回调），含"不知道"的诚实兜底 |
    | sources | array | 引用出处，元素结构 {title, page, score, text}，title 为文档名/链接 |
    | confidence | float | 综合置信度，供转人工判断 |

- 检验标准：RAGAS 四项指标（上下文相关性、上下文召回率、忠实度、答案相关性）+ 引用准确率 + "不知道"兜底正确率 + 生成延迟
- 对应需求：R4

### T8. 实现问答服务 API 接口

- 目标：提供完整 HTTP/WS 接口，串联 T5/T6/T7 路由逻辑，集成 SSO 用户身份与权限；低置信度/投诉类问题转人工并联动工单系统，用户反馈落库回写知识库
- 实现技术方案：FastAPI + WebSocket + Redis 会话缓存；端口 8003，风格对齐现有 `接口文档.md`；请求头携带 SSO token，按用户团队注入权限过滤；MySQL 工单表 + Redis 队列 + 工单系统 API 对接，转人工与反馈闭环收敛到本任务接口层
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| WebSocket 单服务同步流式 | 高峰 LLM 调用阻塞、无优先级 | 异步任务队列 + SSE 替换 WS；网关限流与削峰填谷 |

- 对外接口（**前端与后端并行开发的契约基准，需最先定稿**）：

  - `GET /health`

    无入参。

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | status | string | healthy |

  - `POST /api/create_session`

    无入参。

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | session_id | string | UUID 会话标识 |

  - `POST /api/query`（非流式：闲聊 / FAQ 命中 / 兜底转人工）

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | query | string | 是 | 用户问题 |
    | session_id | string | 否 | 会话 ID，不传自动创建 |
    | source_filter | string | 否 | 系统/团队过滤：infra / backend / frontend / data / ops |

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | answer | string | 回答文本或引导提示 |
    | is_streaming | boolean | 是否需要走流式接口 |
    | need_human | boolean | 是否建议转人工 |
    | sources | array | 引用出处，元素结构 {title, page, score, text} |
    | session_id | string | 会话 ID |
    | processing_time | float | 耗时（秒） |

  - `WS /api/stream`（流式 RAG 主链路）

    发送消息：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | query | string | 是 | 用户问题 |
    | session_id | string | 否 | 会话 ID |
    | source_filter | string | 否 | 系统/团队过滤 |

    接收消息：

    | type | 字段 | 说明 |
    |------|------|------|
    | start | session_id | 连接建立 |
    | token | token | 流式文本片段 |
    | end | is_complete, sources, processing_time | 回答完成，附引用与耗时 |
    | error | error | 错误描述 |

  - `GET /api/sources`

    无入参。

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | sources | array | ["infra", "backend", "frontend", "data", "ops"] |

  - `POST /api/feedback`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | session_id | string | 是 | 会话 ID |
    | query | string | 是 | 对应的问题 |
    | rating | int | 是 | 1–5 评分 |
    | comment | string | 否 | 文字反馈 |
    | need_human | boolean | 否 | 是否要求人工处理 |

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | status | string | success |
    | ticket_id | string | need_human=true 时返回工单号（同步创建外部工单） |

- 检验标准：接口正确性（状态码与字段契约一致）、SSO 身份解析正确性、并发吞吐、端到端响应延迟、兜底触发完整性（低置信度必生成工单）、工单系统对接成功率、反馈落库完整率、工单状态流转
- 对应需求：R6、R7

### T9. 实现前端问答界面

- 目标：对话界面 + 文档引用卡片 + 点赞点踩 + 转人工按钮；预留企业 IM（飞书/企微）机器人接入位
- 实现技术方案：React（复用 `../front/` 工程）+ 打字机流式渲染；桌面端为主（研发场景），移动端适配
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| React Web + WS 直连 | 断线重连体验差；研发更希望在 IM 里直接问 | SSE + 心跳自动重连；飞书/企微机器人接入（消息卡片 + 引用链接） |

- 对外接口：无（消费 T8 接口，反馈与转人工接口已并入 T8）
- 检验标准：交互完整性（引用/反馈/转人工可点可用）、桌面端适配、断线重连、IM 机器人消息可达
- 对应需求：R6、R7

### T10. 构建评测集并评估效果

- 目标：构建 50–100 条高频技术问题评测集，跑 RAGAS 四指标，输出评估报告
- 实现技术方案：ragas + 人工标注标准答案；复用 `rag_qa/rag_assesment/` 流程；高频问题按 T1 真实 FAQ 与历史工单分布抽样
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 单模型自动评估 | 与工程师真实满意度存在偏差 | LLM-as-judge 多模型交叉评分；上线后以点赞点踩在线指标持续校准 |

- 对外接口：无（交付物：`docs/eval/评估报告.md` + `internal_kb_qa/rag_assesment/eval_set.json`）
- 检验标准：RAGAS 四项指标（上下文相关性、上下文召回率、忠实度、答案相关性）+ 高频问题自动解答率 + 人工抽检一致率
- 对应需求：R8

### T11. 完成私有化部署压测与监控

- 目标：docker-compose 一键私有化部署（milvus/mysql/redis/app/web），完成压测、监控与权限隔离验证
- 实现技术方案：docker-compose（参考 `docker/milvus_redis/`）+ locust 压测（已有 `locust_test.py`）+ 结构化日志与审计日志（调用 T14 通用日志接口）
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 单机 docker-compose | 无法弹性扩容、故障自愈能力弱 | Kubernetes + HPA 自动扩缩容；Prometheus/Grafana 链路监控与告警 |

- 对外接口：无（交付物：docker-compose 文件、部署说明、压测报告、权限隔离验证记录）
- 检验标准：一键部署成功率、并发压测吞吐与延迟、故障降级行为（LLM 故障降级 FAQ 直回）、权限隔离正确性、审计日志完整可追溯
- 对应需求：R9

### T12. 完成后端服务搭建

- 目标：完成客户端后端服务骨架与内容模块搭建，为 Web/企业 IM 客户端提供业务内容支撑，并与 T8 问答 API 共享 SSO 鉴权
- 实现技术方案：FastAPI + MySQL + Redis（与 T8 同栈）搭建 `backend` 服务；提供客户端启动配置、公告/帮助内容、用户偏好等基础内容接口；SSO token 鉴权复用 T8 策略；预留与 T8 会话/反馈数据打通
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 单体客户端内容服务 | 内容变更需发版、无法按团队定向投放 | 配置中心动态下发；BFF 按客户端类型隔离；内容灰度发布 |

- 对外接口：
  - `GET /api/client/bootstrap`

    无入参。

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | announcements | array | 公告列表 |
    | help_links | array | 帮助内容链接 |
    | features | object | 客户端功能开关 |

- 检验标准：服务启动与健康检查、接口契约一致性、SSO 鉴权互认、并发可用性
- 对应需求：R6

### T13. 检索策略

- 目标：基于意图分类结果实现 Query 改写与检索策略路由，为 T7 主链路提供多路召回与过滤策略
- 实现技术方案：LLM few-shot + 规则双通道 Query 改写（指代消解、口语化→技术术语、同义扩展）；按 T6 输出类别路由策略（tech 走 T4 混合检索，access_request 按权限/系统过滤，incident 按值班手册优先，chitchat 不检索）；生成 RAG-Fusion 多查询；策略配置化，作为 T7 的可插拔检索增强（T7 MVP 单路跑通后接入，不阻塞 T7 交付）
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 规则 + LLM few-shot 改写 | 改写质量依赖 Prompt，多轮指代易漏 | 检索策略离线评估集自动回归；基于点击/反馈的改写质量在线学习 |

- 对外接口：
  - `query_rewrite(query: str, history: list = None, category: str = "tech") -> RewrittenQuery`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | query | string | 是 | 用户原始问题 |
    | history | array | 否 | 最近 N 轮问答，用于指代消解 |
    | category | string | 否 | T6 输出的意图类别 |

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | rewritten_query | string | 改写后的主查询 |
    | sub_queries | array | 多路召回子查询 |
    | filters | object | 建议的 team/system/security_level 过滤条件 |

  - `build_search_strategy(query: str, category: str) -> SearchStrategy`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | query | string | 是 | 用户问题或改写后问题 |
    | category | string | 是 | T6 输出的意图类别 |

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | strategy | string | faq / hybrid / runbook / none |
    | top_k | int | 召回条数 |
    | use_rerank | boolean | 是否进入 T7 重排 |

- 检验标准：Query 改写可读性与意图保持率、改写后召回命中率、策略路由准确率、对 T7 主链路延迟增量
- 对应需求：R4、R5

### T14. 日志

- 目标：编写通用日志接口，统一全链路请求日志、检索日志、模型调用日志与审计日志的格式与写入方式
- 实现技术方案：Python logging + 结构化 JSON 格式；通用接口统一输出 `timestamp、level、event、trace_id、user、team、query、hit_docs、security_level、latency`；敏感字段写入前脱敏；本地异步写入，预留 ELK/Loki 采集；供 T8、T11、T13 调用
- 可优化点：

| 当前技术 | 当前问题 | 可优化技术 |
|----------|----------|------------|
| 本地结构化文件日志 | 跨服务排障需手动关联、无链路视图 | OpenTelemetry 链路追踪 + ELK/Loki 集中检索；日志采样与分级告警 |

- 对外接口：
  - `log(level: str, event: str, fields: dict = None) -> None`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | level | string | 是 | DEBUG / INFO / WARNING / ERROR |
    | event | string | 是 | 事件名，如 query.start / faq.hit / rag.done / audit.access |
    | fields | object | 否 | 业务字段（自动附加 trace_id、时间戳并执行脱敏） |

    出参：无。

  - `query_audit_logs(filters: dict = None) -> list[AuditLog]`

    入参：

    | 参数 | 类型 | 必填 | 说明 |
    |------|------|------|------|
    | filters | object | 否 | 按 user、team、time_range、event 过滤 |

    出参：

    | 字段 | 类型 | 说明 |
    |------|------|------|
    | records | array | 命中审计日志列表 |

- 检验标准：日志格式一致性、敏感字段漏写率、审计日志可追溯完整率、日志写入对主链路延迟影响
- 对应需求：R7、R9

---

## 5. 任务分工（10 人团队）

> 状态与人员以本表为准；T14 责任人由数据组内部分配后回填。

| 任务编号 | 任务名称 | 所属小组 | 核心交付物 | 依赖上游 | 状态 | 人员 |
|----------|----------|----------|------------|----------|------|------|
| T1 | 采集整理内部技术文档与 FAQ | 数据组 | 文档语料库、FAQ 表、脱敏与权限标注表 | 无 | 未开始 | 郑平高 |
| T2 | 实现多格式文档解析器 | 数据组 | 解析器 load() | T1 | 未开始 | 刘攀 |
| T3 | 实现文档切分与向量化入库 | 算法组 | 切分器 split()、Milvus 入库 insert() | T2 | 未开始 | 江彩亮 |
| T4 | 实现混合检索扩展 | 算法组 | Milvus 向量库（含权限过滤） | T3 | 未开始 | 陈江平 |
| T5 | 实现高频技术 FAQ 精确匹配 | 算法组 | faq_match() 模块 | T1 | 未开始 | 杨国东 |
| T6 | 实现意图分类与路由 | 算法组 | classify() 分类器 | T1 | 未开始 | 王博宇 |
| T7 | 实现重排与生成回答 | 算法组 | RAG 主链路 rag_answer() | T4、T5、T6 | 未开始 | 胡潇潇 |
| T8 | 实现问答服务 API 接口 | 服务组 | API 服务（含 SSO/权限/转人工与工单联动），契约先行 | T7 | 未开始 | 毛鑫泽 |
| T9 | 实现前端问答界面 | 前端组 | Web 问答界面（含 IM 接入位） | T8 的 API | 未开始 | 邓新晓 |
| T10 | 构建评测集并评估效果 | 质量组 | 评测集、RAGAS 评估报告 | T7 | 未开始 | 唐海乘 |
| T11 | 完成私有化部署压测与监控 | 服务组 | docker-compose、压测报告、权限隔离验证 | T8 | 未开始 | 郑平高 |
| T12 | 完成后端服务搭建 | 后端组 | 客户端后端内容搭建 | 无 | 未开始 | 郑平高 |
| T13 | 检索策略 | 算法组 | 检索策略和 Query 改写 | T6 | 未开始 | 胡潇潇 |
| T14 | 日志 | 数据组 | 编写通用日志接口 | 无 | 未开始 | 待定 |

**按人员汇总（一人多任务）：**

| 人员 | 负责任务 | 备注 |
|------|----------|------|
| 郑平高 | T1、T11、T12 | 跨数据组 / 服务组 / 后端组 |
| 刘攀 | T2 | 数据组 |
| 江彩亮 | T3 | 算法组 |
| 陈江平 | T4 | 算法组 |
| 杨国东 | T5 | 算法组 |
| 王博宇 | T6 | 算法组 |
| 胡潇潇 | T7、T13 | 算法组 |
| 毛鑫泽 | T8 | 服务组 |
| 邓新晓 | T9 | 前端组 |
| 唐海乘 | T10 | 质量组 |
| 待定 | T14 | 数据组内部分配 |

**分组结构：**

| 小组 | 成员 | 职责 |
|------|------|------|
| 数据组 | 郑平高、刘攀、待定 | T1–T2 数据与解析、T14 通用日志 |
| 算法组 | 江彩亮、陈江平、杨国东、王博宇、胡潇潇 | T3–T7、T13 检索问答链路 |
| 服务组 | 毛鑫泽、郑平高 | T8、T11 服务与私有化部署 |
| 前端组 | 邓新晓 | T9 界面 |
| 质量组 | 唐海乘 | T10 评估 |
| 后端组 | 郑平高 | T12 客户端后端内容搭建 |

**并行解耦点：** T1、T12、T14 无上游依赖，可与主线并行启动；T8 接口契约一旦定稿，前端组（邓新晓）与算法组/服务组即可完全并行开发；T5、T6 在 T1 交付 FAQ/语料基线后并行；T13 在 T6 完成后与 T7 并行，作为主链路检索增强插件接入。

---

## 6. 需求 → 任务覆盖矩阵

| 需求编号 | 需求描述 | 覆盖任务 |
|----------|----------|----------|
| R1 | 多格式内部文档解析 | T1、T2 |
| R2 | 切分与向量化入库（含权限元数据） | T3 |
| R3 | 高频技术 FAQ 精确匹配 | T1、T5 |
| R4 | 检索增强生成（带文档引用） | T4、T7、T13 |
| R5 | 意图分类与路由 | T6 |
| R6 | 问答接口与前端/IM 入口 | T8、T9、T12 |
| R7 | 转人工与工单联动闭环 | T8、T9、T14 |
| R8 | 评测集与效果评估 | T10 |
| R9 | 私有化部署压测监控 | T11、T14 |

自检：9 条需求全部被覆盖；14 个任务全部回溯到需求（T12 支撑 R6 客户端后端内容，T14 支撑 R7 审计与 R9 监控日志）；每个任务均有对外接口契约或明确标注「无对外接口」。

---

## 7. MVP 量化验收标准（带阈值）

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 高频技术问题自动解答率 | ≥ 80% | T10 评测集 50–100 条高频技术问题，自动正确解答占比 |
| 引用溯源率 | 100% | 每条 RAG 回答携带 ≥1 个文档出处，且引用与答案一致 |
| 权限隔离正确率 | 100% | 用低权限测试账号发起越权检索，越权命中数必须为 0 |
| 平均响应时长 | < 3 秒 | 端到端；流式首 token < 1.5 秒 |
| 转人工兜底率 | 100% | 低置信度 / complaint / incident 问题必须引导转人工，不硬答 |
| RAGAS 忠实度 / 答案相关性 | ≥ 0.8 | ragas 自动评估 + 人工抽检 |
| RAGAS 上下文相关性 / 召回率 | ≥ 0.7 | ragas 自动评估 |
| 并发能力 | 50 并发 p95 延迟 < 5 秒 | locust 压测 |

---

## 8. MVP 边界（明确不做）与风险

**MVP 不做**（后续迭代再说）：

- 不做基于知识库的代码自动生成 / 自动修复 / 自动运维变更（高危操作保留人工）
- 不做生产环境操作联动（重启、发布、改配置）
- 不做多模态输入、语音问答
- 不做大模型微调；不扩展到全公司（先 1–2 个团队/系统试点）

**风险与对策：**

| 风险 | 对策 |
|------|------|
| 敏感信息泄露（密钥/客户数据） | 2.3 脱敏管线 + 权限元数据过滤 + 私有化部署 + 审计日志；验收设"越权命中数=0"硬指标 |
| 文档过期误导（改版后旧文档还在库里） | 文档带 `version/last_updated` 元数据，增量重建 + 失效标记 + 回答标注更新时间 |
| 幻觉误导故障处理（on-call 场景代价高） | 低置信度一律转人工；Prompt 强制"不知道就说不知道"；宁可不答，不错答 |
| 多系统文档碎片化 | 统一索引管线接入 Confluence/Git/接口文档，单一检索入口 |
| 权限越权 | 检索链路强制注入 RBAC 过滤（不可绕过），T11 专项验证 |
| 流量峰值 | FAQ 秒回 + Redis 缓存 + LLM 故障降级 FAQ 直回 |

