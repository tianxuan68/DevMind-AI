# DevMind AI 工作台前后端接口契约

本文是功能页联调契约。前端以 `API_BASE_URL` 为基础地址，默认 `http://localhost:8003`；认证采用 `Authorization: Bearer <access_token>`。除流式问答外，响应统一为：

数据库表、字段、索引与页面最小返回字段见：[工作台数据库设计.md](../database/工作台数据库设计.md)。知识库与文档管理的完整实现约定见：[知识库与文档管理接口契约.md](知识库与文档管理接口契约.md)。检索测试、历史记录、我的收藏与系统设置见：[工作台辅助功能接口契约.md](工作台辅助功能接口契约.md)。

```json
{ "request_id": "req_xxx", "data": {}, "error": null }
```

错误时 `data` 为 `null`，`error` 为 `{ "code": "SESSION_NOT_FOUND", "message": "会话不存在" }`。

## 当前实现状态

`backend/app/api/workspace.py` 已实现会话、消息、WebSocket 流式回答、知识库、文档列表、引用、收藏、用户偏好和检索测试。它使用内存数据，仅用于本地联调。

以下路径仍是生产阶段预留，当前不会返回业务数据：文档上传 `POST /api/v1/documents`、回答反馈 `POST /api/v1/messages/{id}/feedback`。登录、注册、短信验证码和退出登录的完整约定见：[登录页接口契约.md](登录页接口契约.md)；前端在这些端点接入前保留 Mock。

## 1. 功能页按钮与行为

| 页面位置 | 控件 | 前端行为 | 后端接口 |
| --- | --- | --- | --- |
| Sidebar | 新建对话 | 创建空会话，清空消息并切换为当前会话 | `POST /api/v1/sessions` |
| Sidebar | AI 工作台 | 信息分组标签，不是独立功能或接口 | 无 |
| Sidebar | 智能问答 | 路由切换；加载最近会话 | `GET /api/v1/sessions` |
| Sidebar | 知识库 | 进入知识库列表 | `GET /api/v1/knowledge-bases` |
| Sidebar | 文档管理 | 进入文档列表和上传页 | `GET /api/v1/documents` |
| Sidebar | 检索测试 | 进入测试页，提交检索请求时显示片段与重排结果 | `POST /api/v1/retrieval/test` |
| Sidebar | 历史记录 | 按更新时间加载会话列表 | `GET /api/v1/sessions` |
| Sidebar | 我的收藏 | 加载已收藏的回答 | `GET /api/v1/favorites` |
| Sidebar | 系统设置 | 读取和保存个人偏好 | `GET/PATCH /api/v1/me/preferences` |
| Sidebar | 用户菜单 | 获取当前用户、退出登录 | `GET /api/v1/me`、`POST /api/v1/auth/logout` |
| 主区 | 发送问题 | 创建用户消息，连接流式通道；依次显示分析、检索、重排、生成和答案 token | `POST /api/v1/messages` + `WS /api/v1/chat/stream` |
| 主区 | 技术问题 / 问题排查 / 文档总结 | 将预设 prompt 填入输入框并发送；不需要独立后端接口 | 复用消息接口，`mode` 分别为 `tech` / `troubleshoot` / `summarize` |
| 回答卡 | 复制 | 仅调用浏览器 Clipboard API，不请求后端 | 无 |
| 回答卡 | 继续追问 | 将回答作为上下文继续发送新问题 | 复用消息接口，携带 `parent_message_id` |
| 回答卡 | 查看引用 | 展开或聚焦右侧引用面板 | 引用随流式 `final` 事件返回；按需用 `GET /api/v1/messages/{id}/citations` |
| 回答卡 | 收藏 | 切换该回答收藏状态 | `PUT/DELETE /api/v1/favorites/messages/{message_id}` |
| 右侧引用卡 | 点击来源 | 打开文档定位页 / Chunk 原文抽屉 | `GET /api/v1/citations/{citation_id}` |
| Landing | 进入 DevMind AI / 登录 | 未登录进入 `/login`；已登录可直接进入工作台 | 登录页认证接口见 [登录页接口契约.md](登录页接口契约.md) |

## 2. 会话与消息

### 创建会话

`POST /api/v1/sessions`

```json
{ "title": "", "knowledge_base_ids": ["kb_platform", "kb_security"] }
```

```json
{ "request_id": "req_01", "data": { "id": "sess_01", "title": "新建对话", "updated_at": "2026-08-22T13:00:00Z" }, "error": null }
```

### 会话列表和详情

- `GET /api/v1/sessions?cursor=&limit=20`
- `GET /api/v1/sessions/{session_id}`

详情中的 `messages` 需要按时间升序返回。每条 AI 消息必须包含 `id`、`content`、`status`、`citation_count`、`confidence`；不用内嵌完整引用内容，避免历史列表过大。

### 创建用户消息

`POST /api/v1/messages`

```json
{
  "session_id": "sess_01",
  "content": "生产环境如何配置统一身份认证？",
  "mode": "tech",
  "knowledge_base_ids": ["kb_platform", "kb_security"],
  "parent_message_id": null
}
```

```json
{
  "request_id": "req_02",
  "data": {
    "user_message_id": "msg_u_01",
    "assistant_message_id": "msg_a_01",
    "stream_url": "ws://localhost:8003/api/v1/chat/stream?message_id=msg_a_01"
  },
  "error": null
}
```

后端在这里创建两条消息记录，避免流式断开后出现无归属的回答。

## 3. 流式问答协议

连接 `WS /api/v1/chat/stream?message_id={assistant_message_id}`。服务端事件一律为 JSON：

```json
{ "type": "retrieval", "data": { "stage": "search", "message": "正在检索 12 个知识库…", "retrieved_count": 13 } }
```

| `type` | `data` | 前端动作 |
| --- | --- | --- |
| `status` | `{ "stage": "analyzing", "message": "正在分析问题…" }` | 显示思考状态 |
| `retrieval` | `{ "stage": "search|recall|rerank", "message": "…", "retrieved_count": 13, "reranked_count": 5 }` | 点亮 RAG 进度节点 |
| `token` | `{ "content": "增量文本" }` | 追加到 AI 回答卡 |
| `citations` | `{ "items": [Citation] }` | 右侧卡片依次出现 |
| `final` | `{ "message_id": "msg_a_01", "confidence": "high", "citation_count": 5, "usage": { "input_tokens": 0, "output_tokens": 0 } }` | 停止流式、显示操作按钮 |
| `error` | `{ "code": "RAG_FAILED", "message": "检索失败，可重试" }` | 保留用户问题并展示重试按钮 |

断线重连时前端用 `GET /api/v1/messages/{message_id}` 取已落库内容；不要重新提交同一个问题。

## 4. 引用与可信度

### 引用列表

`GET /api/v1/messages/{message_id}/citations`

```json
{
  "request_id": "req_03",
  "data": {
    "answer_basis_count": 5,
    "confidence": "high",
    "items": [
      {
        "id": "cite_01",
        "document_id": "doc_01",
        "document_name": "身份认证接入规范.pdf",
        "document_type": "pdf",
        "location_label": "第 18 页",
        "chunk_id": "chunk_42",
        "score": 0.94,
        "excerpt": "OIDC 接入应统一使用企业 IdP，并结合 MFA 与角色…",
        "url": null
      }
    ]
  },
  "error": null
}
```

`score` 固定为 0 到 1 的数值，前端格式化为百分比；`confidence` 仅允许 `high`、`medium`、`low`。

### 引用详情

`GET /api/v1/citations/{citation_id}` 返回 `document_id`、`location_label`、`excerpt`、`content`、`highlights`、`access_allowed`。无权限返回 `403 DOCUMENT_ACCESS_DENIED`，前端不能展示片段正文。

## 5. 收藏、反馈与个人配置

### 收藏

- `PUT /api/v1/favorites/messages/{message_id}`
- `DELETE /api/v1/favorites/messages/{message_id}`
- `GET /api/v1/favorites?cursor=&limit=20`

收藏接口返回 `{ "message_id": "msg_a_01", "favorited": true }`；按钮可乐观更新，失败时回滚。

### 回答反馈

`POST /api/v1/messages/{message_id}/feedback`

```json
{ "rating": "up", "reason": "", "comment": "" }
```

`rating` 只允许 `up`、`down`、`handoff`。`handoff` 时可额外返回工单 ID。

### 用户与偏好

- `GET /api/v1/me`
- `GET /api/v1/me/preferences`
- `PATCH /api/v1/me/preferences`

偏好字段建议仅保留 `default_knowledge_base_ids`、`default_mode`、`locale`，不在前端存储访问令牌。

## 6. 知识库、文档和检索测试

| 接口 | 用途 |
| --- | --- |
| `GET /api/v1/knowledge-bases` | Sidebar / 选择知识库时的可访问知识库列表，返回 `id`、`name`、`document_count`、`status` |
| `GET /api/v1/documents?knowledge_base_id=&cursor=` | 文档管理列表 |
| `POST /api/v1/documents` | 上传文档，使用 `multipart/form-data` |
| `POST /api/v1/retrieval/test` | 返回 Query 改写、召回片段、重排分数，不生成答案 |

检索测试请求：

```json
{ "query": "统一身份认证", "knowledge_base_ids": ["kb_security"], "top_k": 20, "rerank_top_n": 5 }
```

## 7. 后端实现约束

1. 每个接口都由鉴权层根据用户身份过滤会话、知识库、文档和引用，不信任前端传入的 `knowledge_base_ids`。
2. `session_id`、`message_id` 和 `citation_id` 使用不可枚举 ID；未归属资源返回 404，不泄露存在性。
3. 流式生成按 token 或 20 到 80 个字符小块推送。状态事件的顺序必须是 `analyzing → search → recall → rerank → generating → final`。
4. RAG 检索和生成应写入审计日志：用户、会话、知识库、文档、查询摘要、时间、耗时和结果状态；不要记录原始令牌或敏感正文。
5. 第一阶段无需引入任务队列或复杂工具调用。长文档解析与大文件上传达到容量瓶颈后，再异步化。
